## Packages
framer-motion | Page transitions, playful card interactions, and hover effects
clsx | Conditional class name joining
tailwind-merge | Merging tailwind classes

## Notes
- Using Replit Auth (`/api/login`, `/api/logout`) for authentication
- Pokémon images are provided via the database `imageUrl` field
- Wouter used for routing
- Custom type colors mapped in `lib/pokemon-helpers.ts`
